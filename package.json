{
  "name": "authsdk",
  "version": "1.0.0",
  "description": "A SDK for our auth service",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev": "tsc -p ./tsconfig.dev.json -w",
    "dev:build": "tsc -p ./tsconfig.dev.json",
    "dev:link": "node devlink.js"
  },
  "author": "Lucas Galliot",
  "license": "ISC",
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "jest": "^26.6.3",
    "node-watch": "^0.7.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "axios": "^0.21.1"
  }
}
